---
layout: page
title: Blog
url: /posts
pageOrder: 5
---

- var year = '';
#blog-archives
  each post in getFiles({ layout: 'post', outExtension: 'html' }, { date: -1 }).toJSON()
    if formatDate(post.ctime, 'YYYY') != year
      - year = formatDate(post.ctime, 'YYYY')
      h2= year
    article
      h3
        a(href=post.url)= post.title
      time(datetime=post.ctime.toISOString(), pubdate)
        span.month= formatDate(post.ctime, 'MMM')
        span.day= formatDate(post.ctime, 'DD')
        span.year= formatDate(post.ctime, 'YYYY')
